import XCTest
@testable import StudentProjectDetailFeature

final class StudentProjectDetailFeatureTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(StudentProjectDetailFeature().text, "Hello, World!")
    }
}
