import XCTest
@testable import StudentProfileFeature

final class StudentProfileFeatureTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(StudentProfileFeature().text, "Hello, World!")
    }
}
